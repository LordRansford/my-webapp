---
title: "Digitalisation Intermediate"
description: "We go deeper into data models, APIs, platforms and real energy data flows."
level: "intermediate"
courseId: "digitalisation"
levelId: "intermediate"
summary: "We go deeper into data models, APIs, platforms and real energy data flows."
estimatedHours: 10
stepIndex: 1
---

import ToolCard from "@/components/notes/ToolCard"
import Callout from "@/components/notes/Callout"
import GlossaryTip from "@/components/notes/GlossaryTip"
import QuizBlock from "@/components/notes/QuizBlock"
import PageNav from "@/components/notes/PageNav"
import SectionProgressToggle from "@/components/notes/SectionProgressToggle"
import LevelProgressBar from "@/components/course/LevelProgressBar"
import CPDTracker from "@/components/CPDTracker"
import DiagramBlock from "@/components/DiagramBlock"
import { digitalisationSectionManifest } from "@/lib/digitalisationSections"
import { DataFlowMapperLab } from "@/components/tools/digitalisation/DataFlowMapperLab"
import ApiContractExplorer from "@/components/notes/tools/digitalisation/intermediate/ApiContractExplorer"
import SchemaMappingSandbox from "@/components/notes/tools/digitalisation/intermediate/SchemaMappingSandbox"
import DigitalisationDashboard from "@/components/dashboards/DigitalisationDashboard"

# Digitalisation Intermediate

<LevelProgressBar courseId="digitalisation" levelId="intermediate" sectionIds={digitalisationSectionManifest.intermediate} />

<CPDTracker courseId="digitalisation" levelId="intermediate" estimatedHours={10} />

This level turns foundations into applied design. We move from ideas into data flows, integration choices, and the signals you must monitor in real operations.

---

## Data pipelines and flows

<SectionProgressToggle courseId="digitalisation" levelId="intermediate" sectionId="dig-intermediate-data-pipelines" />

A <GlossaryTip term="pipeline">A sequenced flow that moves data from source to usable output.</GlossaryTip> is only valuable when each step is owned and tested. Pipelines fail quietly when ownership is unclear or data quality is ignored.

I always sketch flows first: where the data starts, where it stops, and how it becomes useful. That makes governance practical, not theoretical.

<DiagramBlock title="Pipeline flow" subtitle="Make each stage visible so failures are easy to find.">
  <div className="flex flex-wrap items-center gap-2 text-xs sm:text-sm">
    <div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-slate-700">Source systems</div>
    <span className="text-slate-400">-></span>
    <div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-slate-700">Ingest and validate</div>
    <span className="text-slate-400">-></span>
    <div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-slate-700">Store and govern</div>
    <span className="text-slate-400">-></span>
    <div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-slate-700">Transform and serve</div>
  </div>
  <div className="mt-3 rounded-xl border border-slate-200 bg-slate-50/70 p-3 text-xs text-slate-700">
    Quality checks and ownership belong to every step, not just the end.
  </div>
</DiagramBlock>

<ToolCard
  id="dig-pipeline-lab"
  title="Pipeline flow mapper"
  description="Map a data flow, name the owners, and flag sensitive steps."
>
  <DataFlowMapperLab />
</ToolCard>

<QuizBlock
  id="dig-intermediate-pipelines-quiz"
  title="Quick check: pipelines and flows"
  questions={[
    { q: "Why sketch a pipeline before building", a: "It exposes owners, dependencies, and data risks early." },
    { q: "Where do most pipeline failures hide", a: "In unclear ownership and weak data quality checks." },
    { q: "What makes a pipeline reliable", a: "Clear validation, lineage, and monitored handoffs." },
    { q: "Why is storage not the same as governance", a: "Data can be stored without clear rules or accountability." },
    { q: "What is a pipeline output", a: "A usable dataset or service ready for decisions." },
    { q: "Why document data lineage", a: "So you can trace issues back to their source." },
  ]}
/>

---

## APIs and system integration

<SectionProgressToggle courseId="digitalisation" levelId="intermediate" sectionId="dig-intermediate-apis-and-integration" />

APIs are the contracts that keep systems aligned. A <GlossaryTip term="webhook">A call a system sends to another system when an event happens.</GlossaryTip> supports real time updates without constant polling.

When integrations are vague, every team invents their own definition and version. The result is slow change and fragile services.

<DiagramBlock title="Integration view" subtitle="Contracts keep services stable as they evolve.">
  <div className="grid gap-2 sm:grid-cols-3">
    <div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-700">
      <p className="font-semibold text-slate-900">Service A</p>
      <p className="mt-1">Publishes changes through an API or webhook.</p>
    </div>
    <div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-700">
      <p className="font-semibold text-slate-900">API gateway</p>
      <p className="mt-1">Auth, throttling, and version control live here.</p>
    </div>
    <div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-700">
      <p className="font-semibold text-slate-900">Service B</p>
      <p className="mt-1">Consumes the contract without guessing fields.</p>
    </div>
  </div>
</DiagramBlock>

<ToolCard
  id="api-contract-explorer"
  title="API contract explorer"
  description="Review a contract and check if it is ready for real integrations."
>
  <ApiContractExplorer />
</ToolCard>

<QuizBlock
  id="dig-intermediate-apis-quiz"
  title="Quick check: APIs and integration"
  questions={[
    { q: "Why do API contracts matter", a: "They keep services consistent and safe as they evolve." },
    { q: "What does a webhook do", a: "Sends an event to another system when something changes." },
    { q: "Why is versioning important", a: "It prevents breaking changes for consumers." },
    { q: "What is the role of an API gateway", a: "Auth, throttling, and access control." },
    { q: "What causes fragile integrations", a: "Unclear contracts and hidden assumptions." },
    { q: "Why define error responses", a: "So consumers can handle failures predictably." },
  ]}
/>

---

## Data models and mapping

<SectionProgressToggle courseId="digitalisation" levelId="intermediate" sectionId="dig-intermediate-models-and-mapping" />

A shared data model keeps systems aligned. <GlossaryTip term="schema mapping">The rules that translate fields from a source schema into a shared model.</GlossaryTip> is where meaning is preserved or lost.

Mapping is a design choice. It decides what is essential, what can be dropped, and what must stay consistent across services.

<DiagramBlock title="Schema mapping" subtitle="Translate once and reuse everywhere.">
  <div className="flex flex-wrap items-center gap-2 text-xs sm:text-sm">
    <div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-slate-700">Source schema</div>
    <span className="text-slate-400">-></span>
    <div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-slate-700">Mapping rules</div>
    <span className="text-slate-400">-></span>
    <div className="rounded-xl border border-slate-200 bg-white px-3 py-2 text-slate-700">Canonical model</div>
  </div>
  <div className="mt-3 rounded-xl border border-slate-200 bg-slate-50/70 p-3 text-xs text-slate-700">
    When mappings change, update downstream consumers and keep a clear version history.
  </div>
</DiagramBlock>

<ToolCard
  id="schema-mapping-sandbox"
  title="Schema mapping sandbox"
  description="Map a source schema into a shared model and check coverage."
>
  <SchemaMappingSandbox />
</ToolCard>

<QuizBlock
  id="dig-intermediate-models-quiz"
  title="Quick check: models and mapping"
  questions={[
    { q: "Why use a canonical model", a: "It reduces translation work and keeps meaning consistent." },
    { q: "What is schema mapping", a: "Rules that translate source fields into a shared model." },
    { q: "What breaks when mappings drift", a: "Dashboards and services start reporting different truths." },
    { q: "Why document mappings", a: "So changes are traceable and safe." },
    { q: "What should mapping decisions consider", a: "Business meaning, not just field names." },
    { q: "When should you version schemas", a: "When changes affect downstream consumers." },
  ]}
/>

---

## Operations, monitoring and observability

<SectionProgressToggle courseId="digitalisation" levelId="intermediate" sectionId="dig-intermediate-ops-and-observability" />

<GlossaryTip term="telemetry">Data that shows how systems behave in real time, such as logs or metrics.</GlossaryTip> is essential for safe operations. <GlossaryTip term="observability">The ability to understand system health by inspecting its outputs.</GlossaryTip> is what lets teams respond before users feel the damage.

Monitoring must cover both speed and safety. If you only watch speed, you miss quality. If you only watch quality, you miss delivery friction.

<DiagramBlock title="Ops signal loop" subtitle="Logs and metrics should lead to action.">
  <div className="grid gap-2 sm:grid-cols-4">
    <div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-700">
      <p className="font-semibold text-slate-900">Logs</p>
      <p className="mt-1">Events and errors with context.</p>
    </div>
    <div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-700">
      <p className="font-semibold text-slate-900">Metrics</p>
      <p className="mt-1">Speed, quality, cost, and adoption.</p>
    </div>
    <div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-700">
      <p className="font-semibold text-slate-900">Dashboards</p>
      <p className="mt-1">Make trends visible to teams.</p>
    </div>
    <div className="rounded-xl border border-slate-200 bg-white p-3 text-xs text-slate-700">
      <p className="font-semibold text-slate-900">Actions</p>
      <p className="mt-1">Triage, fixes, and follow up.</p>
    </div>
  </div>
</DiagramBlock>

<ToolCard
  id="ops-observability-lab"
  title="Observability signal picker"
  description="Choose signals that show both speed and safety."
>
  <DigitalisationDashboard />
</ToolCard>

<QuizBlock
  id="dig-intermediate-ops-quiz"
  title="Quick check: operations and observability"
  questions={[
    { q: "What is telemetry", a: "Data that shows how systems behave in real time." },
    { q: "Why is observability different from monitoring", a: "It helps explain why a system is behaving that way." },
    { q: "What happens when you only track speed", a: "Quality issues stay hidden." },
    { q: "Why should dashboards lead to action", a: "Signals without action do not improve outcomes." },
    { q: "What should ops teams watch", a: "Adoption, quality, cost, and stability together." },
    { q: "Why log context with errors", a: "It makes root cause analysis possible." },
  ]}
/>

---

<PageNav prevHref="/digitalisation/beginner" prevLabel="Foundations" nextHref="/digitalisation/advanced" nextLabel="Advanced" showTop showBottom />
