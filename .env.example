# =============================================================================
# Environment Variables Configuration Example
# =============================================================================
# Copy this file to .env.local for development or configure these variables
# in your production environment (e.g., Vercel, AWS, etc.)
#
# IMPORTANT: Never commit .env.local or any file containing real credentials
# =============================================================================

# -----------------------------------------------------------------------------
# NextAuth Configuration (Required for Authentication)
# -----------------------------------------------------------------------------
# NextAuth requires these environment variables for secure authentication.
# Without these, sign-in/sign-up flows will not work properly.

# The base URL of your application (required for NextAuth to work)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# Secret key for encrypting JWT tokens and session data (required)
# Generate a secure random string: `openssl rand -base64 32`
# MUST be set in production for secure authentication
# SECURITY: Do NOT use the example value below - generate your own!
NEXTAUTH_SECRET=REPLACE_WITH_GENERATED_SECRET_use_openssl_rand_base64_32

# -----------------------------------------------------------------------------
# Google OAuth Provider (Optional - Required only if using Google Sign-In)
# -----------------------------------------------------------------------------
# To enable Google authentication:
# 1. Go to https://console.cloud.google.com/
# 2. Create a project and enable Google+ API
# 3. Create OAuth 2.0 credentials
# 4. Add authorized redirect URI: {NEXTAUTH_URL}/api/auth/callback/google

GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# -----------------------------------------------------------------------------
# Email Provider (Optional - Required only if using Email Magic Links)
# -----------------------------------------------------------------------------
# To enable passwordless email authentication:
# Configure your SMTP server details

# SMTP connection string format:
# smtp://username:password@smtp.example.com:587
# or smtps://username:password@smtp.example.com:465 (for SSL)
EMAIL_SERVER=smtp://user:pass@smtp.example.com:587

# The email address that magic links are sent from
EMAIL_FROM=noreply@yourdomain.com

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# Prisma database connection string
# Development: Use SQLite file:./dev.db
# Production: Use PostgreSQL or other supported database
DATABASE_URL=file:./dev.db

# -----------------------------------------------------------------------------
# Stripe Configuration (Optional - Required for payment processing)
# -----------------------------------------------------------------------------
# Only needed if you're using Stripe for payments or subscriptions

STRIPE_SECRET_KEY=sk-test-replace-with-your-stripe-key
STRIPE_WEBHOOK_SECRET=whsec-replace-with-webhook-secret
STRIPE_TEST_ENABLED=true

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Set to 'production' in production deployments
NODE_ENV=development

# -----------------------------------------------------------------------------
# Optional: Analytics and Monitoring
# -----------------------------------------------------------------------------
# Vercel Analytics (automatically configured on Vercel)
# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=

# -----------------------------------------------------------------------------
# Optional: Feature Flags
# -----------------------------------------------------------------------------
# Enable/disable specific features
# ENABLE_GOOGLE_AUTH=true
# ENABLE_EMAIL_AUTH=false
# ENABLE_STRIPE_PAYMENTS=false

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. Never commit real credentials to version control
# 2. Use strong, randomly generated secrets in production
# 3. Rotate secrets regularly
# 4. Use different credentials for development and production
# 5. Store production secrets in your hosting platform's secure environment
#    variable system (e.g., Vercel Environment Variables)
# =============================================================================
